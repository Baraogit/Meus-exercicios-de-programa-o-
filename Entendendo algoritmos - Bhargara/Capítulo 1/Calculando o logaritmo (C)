#include <stdio.h>
#include <stdlib.h>

int calcularMMC(int logaritmando){
  int cont = 1;
  if (logaritmando == 1)
     return 0;
  else{
    return cont + calcularMMC(logaritmando / 2);
    }
}

int main(){
    int logaritmando;

    printf("Digite o valor do logaritimando: ");
    scanf("%d", &logaritmando);

    printf("Valor do logaritmo = %d", calcularMMC(logaritmando));

