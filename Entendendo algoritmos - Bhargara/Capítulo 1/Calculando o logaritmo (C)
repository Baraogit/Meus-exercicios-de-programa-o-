#include <stdio.h>
#include <stdlib.h>

float fatorarLogaritmando(int logaritmando){
  float cont = 1.0;
  if (logaritmando == 1)
     return 0;
  else{
    return cont + fatorarLogaritmando(logaritmando / 2.0);
  }
}

int main(){
    int logaritmando;

    printf("Digite o valor do logaritimando: ");
    scanf("%d", &logaritmando);

    printf("Valor do logaritmo = %f", fatorarLogaritmando(logaritmando));

  return 0;
}
    
    
----------------------------------------------------------------------------------------------------------
Em que a base do log para cálculo, é sempre 2. 
Motivo? A ideia é saber em quantas tentativas, usando pesquisa binária, serão necessários para descobrir n. 

Em uma pesquisa de n números, a pesquisa binária precisa de no máximo (log(2) n = x) para retornar o número máximo de tentativas até acertar o número.
Em que x é o número de tentativas. 
