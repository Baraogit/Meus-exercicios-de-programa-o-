12) Faça um programa que leia um vetor de 10 posições. Verifique se existem valores
iguais e os escreva na tela.

#include <stdio.h>

int main(){
    int vet[10],  i, j, igual;
    for(i = 0; i < 10; i++){
       printf("Digite um numero: ");
       scanf("%d", &vet[i]);
    }
    for (i = 0; i < 9; i++){
        for (j = i+1; j < 10; j++){
            if (vet[i] == vet[j]){
                printf("Valor que se repete = %d\n", vet[i]);
                break;
            }    
        }
    }
    return 0;
}
-------------------------------------------------------------------------
#include <stdio.h>

int main(){
    int vet[10],repete[10] = {0}, i, j;
    for(i = 0; i < 10; i++){
       printf("Digite um numero: ");
       scanf("%d", &vet[i]);
    }
    for (i = 0; i < 9; i++){
        for (j = i+1; j < 10; j++){
            if (vet[i] == vet[j]){
                repete[i] = vet[i];
                break;
            }
        }
    }
    for (i = 0; i < 10; i++){
         if (repete[i] != 0)
             printf("Valor que se repete = %d\n", repete[i]);
    }
    return 0;
}
-----------------------------------------------------------------
