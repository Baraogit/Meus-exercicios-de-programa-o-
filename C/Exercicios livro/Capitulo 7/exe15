15) Escreva um programa que leia duas strings e as imprima em ordem alfabética, a
ordem em que elas apareceriam em um dicionário.

#include <stdio.h>
#include <string.h>
int main(){
    char palavra1[100], palavra2[100];
    int i, j;

    printf("Digite a primeira string: ");
    gets(palavra1);
    printf("Digite a primeira string: ");
    gets(palavra2);

    for (i = 0,  j = 0; i < strlen(palavra1), j < strlen(palavra2); i++, j++){
        if (palavra1[i] < palavra2[j]){
             printf("1 = %s\n", palavra1);
             printf("2 = %s\n", palavra2);
             break;
        }
        else
            if (palavra2[j] < palavra1[i]){
             printf("1 = %s\n", palavra2);
             printf("2 = %s\n", palavra1);
             break;
        }
    }
    
    -------------------------------------------------------
    Não usa o J. Eu cheguei a ter a ideia de não precisar usar a variável J, mas achei que daria errado. Eu deveria ter testado e verfificado se lógica ainda sim funciona. 
    https://github.com/misaelrezende/Exercicios-do-Livro-Linguagem-C-Completa-e-Descomplicada/blob/master/Capitulo%207/ex15.c
    
    #include <stdio.h>

int main(){
    char palavra1[50], palavra2[50];
    int i;
    fgets(palavra1, 50, stdin);
    fgets(palavra2, 50, stdin);
    
    for(i = 0; palavra1[i] != '\0'; i++){
        // se a letra na palavra1 for menor que a da palavra 2
        // entao a palavra1 vem primeiro no dicionario
        if(palavra1[i] < palavra2[i]){
            printf("\n%s%s", palavra1, palavra2);
            break;
        }
        // se a letra na palavra2 for menor que a da palavra 1
        // entao a palavra2 vem primeiro no dicionario
        else if(palavra1[i] > palavra2[i]){
            printf("\n%s%s", palavra2, palavra1);
            break;
        }
        // se nenhum dos casos for valido, ou seja, as palavras forem iguais
        // apenas percorra a palavra ate encontrar uma diferenca entre elas
    }
    return 0;
}



