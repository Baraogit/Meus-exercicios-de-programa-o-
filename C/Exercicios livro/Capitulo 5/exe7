7) Elabore um programa que peça ao usuário para digitar 10 valores. Some esses
valores e apresente o resultado na tela.

int main()
{
    int i, valor, somaValor = 0;
    
    for (i=1; i <= 10; i++){
        printf("Digite um valor: ");
        scanf("%d", &valor);
        somaValor += valor;
    }
    printf("Soma dos valores digitados = %d", somaValor);

    return 0;
}


-------------------------------------------------------------------
#include <stdio.h>

int main()
{
    int i, valor, somaValor;
    
    for (i=0, somaValor = 0; i < 10; somaValor += valor, i++){
        printf("Digite um valor: ");
        scanf("%d", &valor);
    }
    printf("Soma dos valores digitados = %d", somaValor);

    return 0;
}

------------------------------------------------------------------

#include <stdio.h>

int main()
{
    int i = 0, valor, somaValor = 0;

    while (i < 10){
        printf("Digite um valor: ");
        scanf("%d", &valor);
        somaValor += valor;
        i++;
    }
    printf("Soma dos valors digitados = %d", somaValor); 

    return 0;
}
