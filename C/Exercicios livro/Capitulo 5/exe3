3) Faça um programa que leia um número inteiro N e depois imprima os N primeiros números naturais ímpares.

#include <stdio.h>

int main()
{
    int qtoImpares, i;
    printf("Digite quantos impares voce quer imprimir: ");
    scanf("%d", &qtoImpares);


    for (i = 0; i <= qtoImpares * 2; i++){
        if (i % 2 != 0)
            printf("%d\n", i);
    }
    return 0;
}
