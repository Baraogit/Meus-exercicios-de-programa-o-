7) Faça um programa que leia um valor em reais e a cotação do dólar. Em seguida,
imprima o valor correspondente em dólares.

#include <stdio.h>

int main()
{
    float valor_em_real, cotacao_dolar, valor_em_dolar; 
    printf("Digite respectivamente o valor em real e a cotacao do dolar:\n");
    scanf("%f %f", &valor_em_real, &cotacao_dolar);
    valor_em_dolar = valor_em_real * cotacao_dolar;
    printf("Valor do real convertido em dolar : %.2f\n", valor_em_dolar);
    
    return 0;
}


