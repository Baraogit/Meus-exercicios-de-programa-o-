11) Leia o valor do raio de um círculo. Calcule e imprima a área do círculo correspondente. A área do círculo é A = π * raio2
, sendo π = 3.141592.

#include <stdio.h>
#define PI 3.141592

int main()
{
    float raio, area_circulo; 
    printf("Digite o valor do raio:\n");
    scanf("%f", &raio);
    area_circulo = PI * (raio * raio);
    printf("Valor do area do circulo : %.2f", area_circulo);

                
    return 0;
}
