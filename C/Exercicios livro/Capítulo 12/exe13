13) Faça um programa que leia um arquivo texto contendo os dados de um aluno.
Cada linha do arquivo contém o número de matrícula, o nome e as notas de três
provas de um aluno. Os dados devem ser lidos e armazenados em uma estrutura. Em seguida, exiba os dados lidos em tela.

#include <stdlib.h>
#include <stdio.h>
#include <string.h>

struct cadastro_aluno{
  char nome[50];
  int matricula;
  float nota[3];
} aluno[3];


int main(){
  FILE *arquivo_aluno = fopen("struct_aluno.txt", "r");
  if (arquivo_aluno == NULL){
    perror("Erro na abertura: ");
    exit(1);
  }
  int i = 0;
  while(1){
    fscanf(arquivo_aluno, "%d %s %f %f %f\n", &aluno[i].matricula, &aluno[i].nome, &aluno[i].nota[0], &aluno[i].nota[1], &aluno[i].nota[2]);
    printf("Matricula: %d Nome: %s Notas: %f %f %f\n", aluno[i].matricula, aluno[i].nome, aluno[i].nota[0], aluno[i].nota[1], aluno[i].nota[2]);
    if(feof(arquivo_aluno))
      break;
    i++;
  }

  fclose(arquivo_aluno);

  return 0;
}
