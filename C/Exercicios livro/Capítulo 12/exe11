11) Escreva uma função que receba uma matriz e seu tamanho como parâmetros e
salve-a em um arquivo texto de nome “matriz.txt”’. Cada linha da matriz deve ser
salva em uma linha do arquivo

#include <stdlib.h>
#include <stdio.h>

void alimentarMatriz(int linhas, int colunas,int matriz[][colunas]){
  FILE *arquivo = fopen("matriz.txt", "w");
  if (arquivo == NULL){
    perror("Erro na abertura: ");
    exit(1);
  }
  int i, j;
  for (i = 0; i < linhas; i++){
    for (j = 0; j < colunas; j++){
      scanf("%d", &matriz[i][j]);
      fprintf(arquivo, "%d ", matriz[i][j]);
      if (j == 2)
        fprintf(arquivo, "%c", '\n');
    }
  }
  fclose(arquivo);

}


int main(){
  int matriz[3][3];
  alimentarMatriz(3, 3, matriz);


  return 0;
}
