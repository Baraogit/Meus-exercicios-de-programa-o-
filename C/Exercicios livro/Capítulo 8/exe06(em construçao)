6) Crie uma estrutura representando um aluno de uma disciplina. Essa estrutura
deve conter o número de matrícula do aluno, seu nome e as notas de três provas.
Agora, escreva um programa que leia os dados de cinco alunos e os armazena nessa
estrutura. Em seguida, exiba o nome e as notas do aluno que possui a maior média
geral dentre os cinco.

#include <stdio.h>
#include <stdlib.h>

struct aluno{
    char nome[50];
    int matricula;
    float nota[3];
};

int main(){
    struct aluno coletaDadosAluno[5];
    int i, j, flag = 0;
    float somaNotas[5] = {0}, mediaNotas[5] = {0}, maiorMedia;

    printf("Coleta de dados dos alunos.\n");
    for (i = 0; i < 5; i++){
        printf("Digite o nome do aluno %d : ", i+1);
        setbuf(stdin, NULL);
        fgets(coletaDadosAluno[i].nome, 50, stdin);// coleta os nomes
        printf("Numero da matricula: ");
        scanf("%d", &coletaDadosAluno[i].matricula); // coleta  o número da matrícula 
        for(j = 0;  j < 3; j++){
            printf("Nota %d = ", j+1);
            scanf("%f", &coletaDadosAluno[i].nota[j]); // coleta as notas 
            somaNotas[i] += coletaDadosAluno[i].nota[j]; // soma as notas
        }
    }
    // calcula a media de cada aluno
    for ( i = 0; i < 5; i++){
        mediaNotas[i] = somaNotas[i] / 3; 
    }
    // descobre qual é a maior media 
    maiorMedia = mediaNotas[0];
    for (i = 0; i < 5; i++){
        if (mediaNotas[i] > maiorMedia){
            maiorMedia = mediaNotas[i];
            flag = i; 
        } 
    }
    // imprime o struct-aluno com a maior media 
    printf("\n"); 
    printf("Aluno com a maior media:\n"); 
    printf("Aluno: %s\nMatricula = %d\n", coletaDadosAluno[flag].nome, coletaDadosAluno[flag].matricula);
    printf("Notas:\n");
    for (i = 0; i < 3; i++){
        printf("%.2f\n", coletaDadosAluno[flag].nota[i]);
    }
    printf("Media = %.2f\n", maiorMedia);

 return 0;
}
