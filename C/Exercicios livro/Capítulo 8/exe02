2) Crie uma estrutura para representar as coordenadas de um ponto no plano (posições X e Y). Em seguida, declare e leia do teclado um ponto e exiba a distância
dele até a origem das coordenadas, isto é, a posição (0,0).

#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct plano_cartesiano{
   int posicaoX, posicaoY, distanciaDaOrigem;
};

int main(){
     struct plano_cartesiano posicao;

     printf("Digite as coordenadas do ponto A\n");
     printf("Coordenada X : ");
     scanf("%d", &posicao.posicaoX);
     printf("Coordenada Y : ");
     scanf("%d", &posicao.posicaoY);

     posicao.distanciaDaOrigem = sqrt(pow( (posicao.posicaoX - 0), 2) + pow( (posicao.posicaoY - 0), 2) );

     printf("Distancia do ponto A para a origem = %d\n", posicao.distanciaDaOrigem);

 return 0;
}

