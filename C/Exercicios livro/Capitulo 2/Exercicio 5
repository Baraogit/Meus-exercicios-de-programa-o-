5) Faça um programa que leia um valor do tipo float e depois o imprima usando o
operador “%d”. Veja o que aconteceu.

#include <stdio.h>
#include <stdlib.h>

int main()
{
    float numero;
    printf("Digite um numero em ponto flutuante e imprima esse valor em inteiro: ");
    scanf("%f", &numero);
    printf("Valor : %d", numero);

    return 0;
}
