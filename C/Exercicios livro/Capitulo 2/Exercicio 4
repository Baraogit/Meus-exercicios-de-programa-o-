#include <stdio.h>
#include <stdlib.h>


int main()
{
    int numero;
    printf("Digite um numero inteiro e imprima esse numero com saída do tipo float:");
    scanf("%d", &numero);
    printf("Numero lido : %f", numero);

    return 0;
}
