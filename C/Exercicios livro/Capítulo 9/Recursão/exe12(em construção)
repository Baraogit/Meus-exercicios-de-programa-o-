12) Escreva uma função recursiva que receba um valor inteiro em base decimal e o imprima em base binária.













------------------------------------------------------------------------
Em construção - forma iterativa 

#include <stdio.h>

int main(){
  int numero, i, resto, cont = 0, cloneValorNumero;

  printf("Digite um valor numero para conversao em binario: ");
  scanf("%d", &numero);
  cloneValorNumero = numero;

  for(cloneValorNumero; cloneValorNumero > 0; cloneValorNumero /= 2){
    cont++; // conta quantos loops houveram, a quantidade será a qto do array
  }
 /* i tem o papel de começar o array de tras para frente, para quando for impresso
 imprimir a sequência correta do número binário*/
  int binario[cont];
  for(i = cont - 1, numero; numero > 0; numero /= 2, i--){ 
    resto = numero % 2;
    if (resto == 0)
       binario[i] = 0;
    else
       binario[i] = 1;
  }
  for (i = 0 ; i < cont; i++){
    printf("%d", binario[i]);
  }
   return 0;
}
