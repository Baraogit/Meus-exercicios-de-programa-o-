12) Escreva uma função recursiva que receba um valor inteiro em base decimal e o imprima em base binária.













------------------------------------------------------------------------
Em construção - forma iterativa 

#include <stdio.h>

int main(){
  int numero, i, resto, cont = 0;

  printf("Digite um valor numero para conversao em binario: ");
  scanf("%d", &numero);
 
  printf("Convertendo...\n");
  for(numero; numero > 0; numero /= 2){
    resto = numero % 2;
    if (resto == 0)
       printf("0");
    else
       printf("1");
       cont++; // conta quantos loops houveram
  }
  printf("\n");
  printf("Digite um valor numero para conversao em binario: ");
  scanf("%d", &numero);
  int binario[cont];
  for(i = 0, numero; numero > 0; numero /= 2, i++){
    resto = numero % 2;
    if (resto == 0)
       binario[i] = 0;
    else
       binario[i] = 1;
  }
  printf("Imprime ao contrario\n");
  for (i = cont - 1 ; i >= 0; i--){
    printf("%d", binario[i]);
  }

   return 0;
}
