1) Escreva uma função recursiva que calcule a soma dos primeiros n cubos:
S = 13 + 23 + ... + n3

#include <stdio.h>

int calcularNcubos(int n){
    int paraDevolver;
    
    if (n == 1)
       return 0; 
    else
       paraDevolver =  n * n * n  + calcularNcubos(n - 1);
       return paraDevolver;
}


int main(){
    int limite, resultado ; 
    printf("Digite o limite do calculo : ");
    scanf("%d", &limite);
    
    resultado = calcularNcubos(limite);
    
    printf("%d", resultado); 

    return 0;
}
