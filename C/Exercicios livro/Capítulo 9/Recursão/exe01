1) Escreva uma função recursiva que calcule a soma dos primeiros n cubos:
S = 13 + 23 + ... + n3

#include <stdio.h>

int calcularNcubos(int n){
    int devolveResultado;
    
    if (n == 1)
       return 0; 
    else
       devolveResultado =  n * n * n  + calcularNcubos(n - 1);
       return devolveResultado;
}


int main(){
    int limiteNcubos, resultadoDaRecursao ; 
    printf("Digite o limite do calculo de n cubos: ");
    scanf("%d", &limiteNcubos);
    
    resultadoDaRecursao = calcularNcubos(limiteNcubos);
    
    printf("%d", resultadoDaRecursao); 

    return 0;
}
