Receber valor de um carro, e criar perguntas sobre o seu estado. Dependendo do estado, criar um meio em que ele vai desvalorizando conforme:
quilometragem, se teve batida, se foi roubado, se está com problema e etc. 
Criar uma estrutura com suas características: modelo,placa e ano. 

#include <stdio.h>

struct automovel{
    char modelo[50];
    int placa, ano; 
    float valor; 
};

int main(){
    struct automovel carro;
    int opcao; 
    
    
    printf("Digite o modelo do carro: ");
    fgets(carro.modelo, 50, stdin);
    printf("Digite placa, ano e valor atual: ");
    scanf("%d %d %f", &carro.placa, &carro.ano, &carro.valor);
    printf("\n");
    printf("\n");
    
    printf("Escolha o que voce deseja informar sobre seu carro:\n");
    do{
        printf("1 - Houve batida.\n");
        printf("2 - Quilometragem.\n");
        printf("3 - Ha algum defeito.\n");
        printf("4 - Ja foi roubado.\n");
        printf("5 - Sair\n");
        printf("\n");
        printf("Escolha sua opção: ");
        scanf("%d", &opcao);
        
        switch (opcao){
            case 1: 
        }
    }while(opcao <= 4);

    return 0;
}
