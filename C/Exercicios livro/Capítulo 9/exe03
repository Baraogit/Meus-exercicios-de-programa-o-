3) Escreva uma função que receba por parâmetro uma temperatura em graus Fahrenheit
e a retorne convertida em graus Celsius. A fórmula de conversão é: C = (F – 32.0) *
(5.0/9.0), sendo F a temperatura em Fahrenheit e C a temperatura em Celsius.


#include <stdio.h>

void converteEmCelsius (float funcTempFahrenheit){
    float celsius;
    celsius = (funcTempFahrenheit - 32.0) * 5.0 / 9.0;
    printf("Valor convertido para celsius = %.2f\n", celsius); 
}

int main(){
    float tempFahrenheit; 
    printf("Digite a temperatura em fahrenheit: ");
    scanf("%f", &tempFahrenheit);
    
    converteEmCelsius(tempFahrenheit);

    return 0;
}
