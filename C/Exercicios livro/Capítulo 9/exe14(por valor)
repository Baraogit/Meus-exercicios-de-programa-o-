14) Escreva uma função que receba um número inteiro positivo n. Calcule e retorne
o seu fatorial n!: n! = n * ( – 1) * (n – 2) * ... * 1.

#include <stdio.h>

calculafatorial (int limite){
    int i, resultado = 1; 
    for (i = 1; i <= limite; i++){
        resultado *= i; 
    }
    printf("Resultado do fatorial = %d", resultado); 
}

int main(){
    int numero; 
    
    printf("Digite um numero para calcular o fatorial: ");
    scanf("%d", &numero); 
    
    calculafatorial(numero); 
  
    return 0;
}
