7) Uma empresa vende o mesmo produto para quatro diferentes estados. Cada estado possui uma taxa diferente de imposto sobre o produto. Faça um programa
em que o usuário entre com o valor e o estado de destino do produto e o programa retorne o preço final do produto acrescido do imposto do estado em que
ele será vendido. Se o estado digitado não for válido, mostrará uma mensagem
de erro.
Estado  MG  SP   RJ   MS
Imposto 7%  12%  15%  8%


#include <stdio.h>

int main()
{
    float valorProduto; 
    short int cod_estado; 
    
    printf("Digite o valor do produto: ");
    scanf("%f", &valorProduto);
    
    printf("Cod. 1 - Tarifa estadual : MG\n");
    printf("Cod. 2 - Tarifa estadual : SP\n");
    printf("Cod. 3 - Tarifa estadual : RJ\n");
    printf("Cod. 4 - Tarifa estadual : MS\n");
    printf("\n"); 
    
    printf("Informe o codigo do estado de destino: ");
    scanf("%d", &cod_estado); 
    
    switch (cod_estado){
        case 1: printf("Valor do produto + roubo : %.2f ", (valorProduto * 7 / 100) + valorProduto);
                break; 
        case 2: printf("Valor do produto + roubo : %.2f ", (valorProduto * 12 / 100) + valorProduto);
                break;
        case 3: printf("Valor do produto + roubo : %.2f ", (valorProduto * 15 / 100) + valorProduto);
                break;
        case 4: printf("Valor do produto + roubo : %.2f ", (valorProduto * 8 / 100) + valorProduto);
                break;
        default: printf("Valor do produto + sonegação: %.2f", valorProduto);
    }
    return 0;
}





