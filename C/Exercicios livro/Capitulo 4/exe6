6) Faça um programa que receba a altura e o sexo de uma pessoa e calcule e mostre
seu peso ideal, utilizando as seguintes fórmulas (em que “h” corresponde à altura):
 • Homens: (72,7 * h) – 58
 • Mulheres: (62,1 * h) – 44,7
 

#include <stdio.h>
#include <stdlib.h>

int main()
{
    float altura, peso_ideal;
    char sexo; 
    
    printf("digite sua altura: ");
    scanf("%f", &altura);
    setbuf(stdin,NULL);// limpar o buffer do teclado, quando não se usa, no proximo scanf usará a sujeira do buffer 
    printf("digite seu sexo (m/f): ");
    scanf("%c", &sexo); 
    
    if (sexo == 'm'){
        peso_ideal = (72.7 * altura) - 58;
        printf("Seu peso ideal = %.2f", peso_ideal);
    }
    else{
        if (sexo == 'f'){
            peso_ideal = (62.1 * altura) - 44.7;
            printf("Seu peso ideal = %.2f", peso_ideal);
        }
    }
  
    return 0;
}
