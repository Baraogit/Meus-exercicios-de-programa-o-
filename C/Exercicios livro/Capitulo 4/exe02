2) Faça um programa que leia dois números e mostre o maior deles. Se, por acaso, os
dois números forem iguais, imprima a mensagem “Números iguais”.

#include <stdio.h>

int main()
{
    short int numero1, numero2;
    printf("Digite dois numeros:\n");
    scanf("%d %d", &numero1, &numero2);
    
    if (numero1 != numero2)
        if (numero1 > numero2)
            printf("%d eh o maior\n", numero1);
        else
            printf("%d eh o maior\n", numero2);
    else{
        printf("Ambos são iguais\n");
    }
   
    return 0;
}

// Sem saber, resolvi o segundo exercicio no primeiro. hehe
